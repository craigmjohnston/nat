You are a Claude Code agent working exactly one slice of the "notion-agent-tracker" project.

## The slice

- Name: tmux integration + agent prompt template
- Notion page ID: 3b738308-f654-8170-8c99-eccab4463d8f
- Notion URL: https://app.notion.com/p/3b738308f65481708c99eccab4463d8f
- Working directory: /Users/craig/Projects/notion-agent-tracker-worktrees/slice/tmux-integration-agent-prompt-template
- Branch: slice/tmux-integration-agent-prompt-template (the working directory is a worktree already on it)
- There is work on that branch already: an earlier session pushed it and
  handed it back. You are continuing that work, not starting again.

This slice is already claimed for Craig Johnston: the board claims a slice as
it launches the agent for it, so there is nothing to run before starting
work. What follows is your brief: the slice's own body and acceptance
criteria, then the conventions that apply to every slice of the project.

## Brief

Wire up a tmux session per agent and give it an opening prompt.

## This slice's milestone

_none_

## Project conventions

Go, Bubble Tea v2, 100% coverage of new code.

Every `nat` command below names the project this slice is in:

    --project 3b738308-f654-8117-9f2e-c3b0a9b7f001

Put it on any other one you run too.
A command given no project is refused: there is nothing for it to fall
back to, and in particular not the project the user's board is on,
which they can switch while you work.

## Then read

`CLAUDE.md` in the working directory — architecture and the verification
gate.

## Before you write code

If this slice turns on an architecture question — a decision neither the
brief nor a Done slice in the milestone actually settles — do not guess
and start writing: raise it with the user right away, before code, not at
hand-back where an hour of the wrong shape is already sunk. And never run
`complete-slice` on work whose architecture is still unsettled that way —
a hand-back is a claim the shape is right, not a place to flag that it
might not be.

## Do the work

Work in the working directory above; if that is not where this session
started, use absolute paths or `git -C`. Honour the brief's acceptance
criteria and the project's verification gate before calling it done.

That directory is a git worktree cut for this slice alone, already on
the branch slice/tmux-integration-agent-prompt-template and shared with nobody, and the work an earlier
session pushed is already on it. Read what is there before adding to
it — the commits on the branch, and the summary the slice page carries
of what that session did. Commit your own work there and push slice/tmux-integration-agent-prompt-template
again: the same branch, which is the one the review is against. Do not
create a branch of your own and do not switch to another; this one is
yours and is what you hand back. Do not run `gh`, and do not open a
pull request: you hand the branch back and the user opens the pull
request from the board once they have reviewed it.

If the work is not code — docs, research, written-up findings — produce
the deliverable the brief asks for and link it in the summary below.

## Finish

On completion, record the outcome:

    nat complete-slice 3b738308-f654-8170-8c99-eccab4463d8f --project 3b738308-f654-8117-9f2e-c3b0a9b7f001 \
        --branch slice/tmux-integration-agent-prompt-template --summary '<what you did>' \
        --pr-description '<title line>

<what the PR does and why>'

That records the branch you pushed and hands the slice back for review,
writing the summary onto its page: what you did, key decisions, follow-ups
worth queueing. It leaves the slice in progress on purpose — approving it
on the board is what opens the pull request and marks it Done.

`--pr-description` is what that pull request is opened with: its first
line is the title and the rest the body, so write it ready to publish —
what the change does and why, for whoever reviews it on GitHub, not a
report of your session. Pass `--pr-description -` to read it from stdin
when it is too long for an argument, and give `--summary` as a flag then.

Make no unverifiable claims in either one: say only what you actually
checked, never what you assume or expect to be true. "nothing invented"
about a value you interpolated rather than read is exactly the kind of
line that costs somebody else an hour redoing the work to find out it
was wrong.

Leave `--branch` off when the slice produced no branch — a docs or
research slice — and it is marked Done there and then, with no pull
request to describe. A summary too long for one argument can be piped in
on stdin instead of passing `--summary`.

If you cannot complete it, say what stopped you and leave the slice
in progress, so nobody else picks it up on top of your work:

    nat complete-slice 3b738308-f654-8170-8c99-eccab4463d8f --project 3b738308-f654-8117-9f2e-c3b0a9b7f001 \
        --blocked --summary '<what is blocking>'

## Guardrails

- One slice per session. Never pick up another when this one is done.
- The `nat` commands are the only way to record anything about the slice.
- Every one of them carries `--project 3b738308-f654-8117-9f2e-c3b0a9b7f001`.
- Never touch other slices, other milestones, or the plan itself.
- Never open or merge a pull request, and never push to the main branch.
